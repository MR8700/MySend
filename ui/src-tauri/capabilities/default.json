{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the NOVA Chat main window: only the app's own custom commands, no filesystem/shell/network plugin access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
