{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the NOVA Chat main window: the app's own custom commands, plus write-only filesystem access scoped to the Downloads directory for the QR-code \"save image\" button (see saveQrImage() in ui/public/app.js) — no shell/network plugin access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "fs:allow-write-file",
      "allow": [{ "path": "$DOWNLOAD/*" }]
    }
  ]
}
